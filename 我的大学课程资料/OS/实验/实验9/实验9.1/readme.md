# 概述
Windows 控制台程序 page-replace-algorithm 项目。主要用于实现最佳页面置换算法、先进先出页面置换算法、最近最久未使用页面置换算法等。  
运行程序后，会将对应的内容打印显示。

# 使用方法
使用为 Linux 0.11 定制的 VSCode 克隆到本地。  
在 VSCode 的 “Terminal” 菜单中选择 “Run Build Task...”，会在 VSCode 的顶部中间位置弹出一个可以执行的 Task 列表，选择其中的 “生成项目”。  
按 Ctrl + F5 运行程序。  